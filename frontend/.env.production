# Production API endpoint
# In CI/CD (GitHub Actions) this value is injected via the VITE_API_URL repository variable.
# If building locally for production, set this to your Azure App Service backend URL:
#   VITE_API_URL=https://YOUR_BACKEND_APP_NAME.azurewebsites.net/api
VITE_API_URL=
